apiVersion: v1
kind: Service
metadata:
  name: postgres-db
spec:
  type: NodePort
  selector:
    app: worker-pool-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
      nodePort: 30100