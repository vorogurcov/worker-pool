apiVersion: v1
kind: Service
metadata:
  name: worker-pool-service
spec:
  type: NodePort
  selector:
    app: worker-pool
  ports:
    - protocol: TCP
      port: 9999
      targetPort: 9999
      nodePort: 30099